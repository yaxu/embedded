-- happy 909 day

d1 $ (runWith 4 (density 1.5) $ stut 2 0.9 0.5 $ slowspread (chop) [8,64,128] $ sound "[bd9*4, rs9*2]")
  -- # speed (slow 4 $ scale 1 0.5 sine1)
  # n (slow 12 $ run 8)
  # orbit "1"
  # room "0.3"
  # size "0.6"
  # gain "0.9"

d2 $ stack [whenmod 16 8 (const silence) $ every 4 (density 1.5) $ sound "{hh9*2 oh9}"
            # speed "1.5"
            # cut "1" # gain "0.9",
            every 2 (density 2) $ sound "{lt9*2 mt9 ht9*3}%2"
            # speed "1.5"
            # orbit "0"
            # shape "0.5"
           ]
  # orbit "0"

d3 $ runWith 4 ((# crush "5") . density 0.5) $ sound "bd9:6(3,8)"
  # shape "0.8"
  # orbit "0"

d4 $ sound "bd9*4? [cr9 [~ cr9]]/2"
  # crush "2"
  # speed "2"
  # cutoff (do n <- discretise 1 $ slow 4 $ scale 1 4 saw1
               slow (realToFrac n) $ scale 1000 4000 saw1
           )
  # resonance "0.2"
 
d5 $ slow 2 $ (jux rev $ up (offadd 0.25 7 $ slow 4 $ "0(5,8) 5(3,8) 7(3,8) 12(5,8)") |*| sound "rs9")
  # gain "0.9" |*| speed "0.5"
  # orbit "1"
  # room "0.3"
  # resonance "0.3"
  # cutoff (slow 4 $ scale 1000 3000 sine1)

