d1 $ jux (rev) $ palindrome $ n "[[4 ~ 4] 8] [15 ~ [15 3]]" # s  "claus"
  # speed (overlay "1" (slow 4 $ scale 1 1.2 sine1))
  # room "0.3"
  # orbit "2"
  
d2 $ sound "[drum cp8]"
  # orbit "1"

d3 $ jux rev $ slow 4 $ n $ ((+) <$> "0" <*>) $ offadd 0.125 12 "c4(3,8) f3*3 g4(3,8) e3*3") # s "xx"

