cps 0.8


-- face arpeggio

-- comb delay drop

-- sine nudge chop

-- arpy weave II

-- slow tabla

-- mismatch step tempo

-- gabba 2s

d1 $ iter 4 $ jux rev $ n ((offadd 0.125 12) $ offadd 0.25 7 $ slow 4 $ "f6(3,8) a6(5,8,2) c6(3,8) e6(5,8,4)") # sound "[rash, jx]"
  # cutoff (scale 100 3000 sine1)
  # resonance "[0.1 0.3]"
