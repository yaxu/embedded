cps 0.9


-- face arpeggio

d1 $ jux rev $ n ((subtract 12) <$> (offadd 0.25 12 $ slow 4 $ offadd 0.125 7 $ palindrome "f8(3,8) a7(3,8) c7*2 e8(3,8,2)")) # sound "jx"
  # cutoff (slow 4 $ scale 400 2000 sine1)
  # resonance "0.2"

d2 $ (jux (iter 4) $ sound "[bd8(3,8) jx:48*2, ~ sd8]")
  # delay "0.3" # delaytime "0.06"
  # vowel "a e i o u "

-- comb delay drop

-- sine nudge chop

-- arpy weave II

-- slow tabla

-- mismatch step tempo

-- gabba 2s


