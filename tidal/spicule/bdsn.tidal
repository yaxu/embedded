
let bdbuild = sound "tok(3,8)"
              # room (slow 16 saw1)
              # size (slow 16 saw1)
              # orbit "0"
              # delay (slow 16 saw1)
              # delaytime "0.25"
              # delayfeedback (scale 0 0.9 $ slow 16 saw1)
              # gain "1.1"

do cps 1.05
   d1 $ (nextSam (now'-1.01)) ~>
     (bdbuild
     )


