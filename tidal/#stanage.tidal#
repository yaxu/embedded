d2 $ sound "claus"
d1 $ iter 2 $ sometimes (|+| n "-12") $ (n (slow 4 $ offadd 0.125 (12) $ offadd 0.25 5 $ "c3(3,8) d3*2 g3(3,8) e3*2")) # s "xx"
  # gain "0.9"
  # orbit "2" # room "0.5"

d2 $ sound "bd*2 [bd ~ sn:2]"
  # orbit "1"

d3 $ slowspread ($) [id,density 2, rev,iter 4] $ slow 2 $ jux (iter 4) $ striate 16 $ sound "arc"
  # orbit "1"

d5 $ sometimes (rev . chop 8) $ sometimes (|+| speed "2") $
  jux (0.25 <~) $ sound (samples "[ifdrums*8, future*8]" (run 8))
  # nudge "[0 0.04]*4" # shape "0.2"

