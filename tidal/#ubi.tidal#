
d1 $ do slow 2 $ degrade $ n (sometimes (+ 12) $ offadd 0.125 12 $ spread (\t -> off t (+12)) [0.25,0.125,0.5] $ "c6 a5 <f5 ! g5 ! e6 !> ~") # sound "rash"
          # lpf (slow 8 $ sine1 * 1000 + (slow 2 $ saw1 * 2000))
          # lpq (slow 4 $ sine1 * 0.3)
          # end 1
          # room (slow 2 $ saw1 * 0.5 + 0.5)
          # size 0.99
          # orbit 1
          # gain "0.9"


d2 $ density' "<1 2>" $ off 0.5 (slow 2 . spread (chop) [3,32,16]) $ jux (0.125 <~) $ off 0.125 (# crush 4) $ off 0.25 (# (speed 2 # vowel "a e")) $ runWith 4 (density 2) $ sound "drum future:1*2"
  # speed 1.5
  # shape 0.7



-- not cycling properly
let off' :: Pattern Time -> (Pattern a -> Pattern a) -> Pattern a -> Pattern a
    off' tp f p = unwrap $ (\tv -> superimpose (f . (tv ~>)) p) <$> tp
    
