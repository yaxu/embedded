d1 $ stack [-- ghost $ sound "[feel feel:2*2@3 feel feel:4]",
  slow 2 $ jux rev $ n (offadd 0.125 12 $ offadd 0.25 7 $ slow 4 $ every 4 rev $ "[f0 g0]/2(3,8) a0(3,8) c0(3,8) e0*2") # s "xx"
            # cutoff (slow 4 $ scale 1000 3000 sine1)
            # resonance "0.3"
            # room "0.9"
            # size "0.9"
            # orbit "1"
           ]

d2 $ sound "drum cp"

d3 $ (jux (iter 4) $ slow 4 $ chop 128 $ sound "shackup"
  # gain "1.1")

d4 $ jux (rev) $ degrade $ slow 2 $ chop 8 $ sound "fm:10"
  # speed "[1, 1.5]"
  # gain "1.1"

hush


d1 $ n (offadd 0.125 12 $ offadd 0.25 (7) 4 $ slow 4 "f0(3,8) a0 c0 e0") # s "xx"