-- Making Spicule http://www.pledgemusic.com/projects/spicule

d1 $ runWith 4 (density 2) $ slowcat [off 0.125 (# speed "2") $ jux (rev) $ slow 2 $ chop 8 $ sound "drum [voodoo:1*2]@3",
                                      off 0.125 (# speed "1.5") $ jux (rev) $ slow 2 $ chop 4 $ sound "drum [voodoo:2*2]@3"
                                     ]

d2 $ palindrome $ chop 2 $ loopAt (1/2) $ sound "~ wobble"
  # speed "6"
  # accelerate "-2"

d3 $ slowspread (\x -> off x (density 2 . (# gain "0.9") . (|*| speed "1.5"))) [0.25, 0.5, 0.75] $ within (0,0.5) (iter 4) $ sound "drum:5 [drum:1 [arpy arpy:3 arpy arpy:5]/4]"
  # end "0.3"
  # resonance "0.2"
  # cutoff (slow 4 $ scale 300 2000 saw1)

x4 $ jux rev $ slowspread (chop) [3,2,6,8] $ sound "jungbass:1 [bd*2 cp]"
  # gain "1.1" # speed (liftA2 (+) "[1 1.1 1.4]/3" "[1 3]/2")
  # crush (liftA2 (+) "[5 3 4]/3" "[1 0]/2")


-- ok stream seems better now.. anyone locked in? @yaxu

d1 $ (chop 64 $ sound "[sd8:3@3 sd8:5, sd8*4, ~ cp8:2]")
  # nudge (slow 4 sine1)