

d1 $ jux (rev . iter 4) $ chop 8 $ sometimes (# vowel "a") $  every 4 (density 1.5) $ s "claus*8" # n (irand 4)
  # size "0.5"
  # room "0.5"
  # orbit "2"
  
d2 $ sound "~ ~ ~ bd*2"
  # gain "1.1"
  # orbit "2"

d3 $ every 4 (slchop 16) $ sound "~ sn:2"