let jux f p = stack [p |+| pan (pure (5/8)), f $ p |+| pan (pure (1/8))]

-- number 9

d1 $ runWith () $ off 0.25 (# crush "<3 4 5>") $ iter 4 $ stut' 4 (1%32) ((|+| pan (slow 8 sine1)) . (|+| speed "[0.1 0.3]*4")) $ n "0(3,8)" # s "jvr:4 mt8"
      # speed "4"

-- tabby

-- comb delay drop

-- sine nudge chop

-- repetition

-- deconstructed liquid gabba
