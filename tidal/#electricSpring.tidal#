let jux f p = stack [p |+| pan (pure (5/8)), f $ p |+| pan (pure (1/8))]

-- number 9

-- mismatch step tempo

-- tabby

d1 $ do x <- "<8 4 8>"
        tabby x (n (run 8) # sound "voodoo") (n (run 8) # sound "")

-- comb delay drop

-- sine nudge chop

-- repetition

-- deconstructed gabba jungbass 6 at last
