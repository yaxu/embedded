let jux f p = stack [p |+| pan (pure (5/8)), f $ p |+| pan (pure (1/8))]

-- number 9

d1 $ off 0.25 () $ stut' 8 (1%32) ((|+| pan (slow 8 sine1)) . (speed "[0.1 0.2]*4" |+|)) $ sound "<bd sn:2 ~>"
      # speed "1"

-- tabby

-- comb delay drop

-- sine nudge chop

-- repetition

-- deconstructed liquid gabba
