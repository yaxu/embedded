import Sound.Tidal.Vis
import qualified Graphics.Rendering.Cairo as C 
import Data.Colour
import Data.Colour.Names
import Data.Colour.SRGB

let fn = "out.svg"
    vis pat = vLines (C.withSVGSurface) fn (600,40) pat 1 1
    visgrid pat = vLines (C.withSVGSurface) fn (600,600) pat 10 20
import Sound.Tidal.Vis
import qualified Graphics.Rendering.Cairo as C 
import Data.Colour
import Data.Colour.Names
import Data.Colour.SRGB
import System.Cmd
let vis pat = do vLines (C.withSVGSurface) "nime.svg" (400,400) pat 10 10
                 rawSystem "/home/alex/Dropbox/bin/fixsvg.pl" ["nime.svg"]
                 rawSystem "convert" ["nime.svg", "nime.pdf"]
                 return ()


vis "black white grey"