(click1, _) <- superDirtSetters getNow

click1 silence

click1 $ n "0 1 1 1" # sound "casio"
  # orbit "2"
  # gain "1.3"

cps 0.9



d1 $ degrade $ (sometimes (# speed "2") $  jux rev $  slow 2 $ n (offadd 0.125 12 $ offadd 0.25 7 $ slow 2 $  "[[f7 g7](6,16)]/2 d7(3,8) e7(3,8) a5(3,8)") # sound "rash")
  # cutoff (slow 4 $ scale 500 5000 sine1)
  # resonance "0.3"

d2 $ runWith 4 (density 2) $ jux rev $ slowspread (chop) [4,8,16] $ sound "[gabba, wobble](3,8)"


d3 $ sound "~ off"

d1 $ runWith 4 (density 2) $ every 4 (# speed "0.5") $ slowspread (chop) [128, 64, 32] $ sound "bd [~ drum] . feel mt ~ drum"
  # delay "0.9"
  # delaytime (every 4 (rev) $ scale 0.01 0.001 sine1)
  # delayfeedback "0.9"
  
d2 $ sound "drum(3,8)"  

d3 $ every 2 (slow 2) $ jux rev $ weave 16 (n (run 8) # sound "arpy*8")
  [vowel "a i u i",
   vowel "a o u",
   speed "1 2 3",
   speed "2 0.5 1 1.5"
  ]
  # orbit "1"
  # gain "1.2"
  |*| speed "[0.5,1]"
y
d4 $ every 4 (# speed "0.5") $ iter 4 $ jux rev $ loopAt 2 $ chop 8 $ sound "arc"
  # shape "0.6"

d5 $ every 2 ((|*| speed "2")) $ jux (rev) $ every 2 (|+| up "12") $ up "0 7 ~ 3" # sound "bass3"
  # shape "0.3"

d2 $ brak $ sound "bd*2 mt ~ mt bd rs ~ sn:2"

d3 $ (jux rev $ iter 4 $ loopAt 8 $ chop 32 $ sound "liq")
  # gain "0.9"
  # cutoff (slow 4 $ scale 2000 4000 sine1)
  # resonance "0.2"

d4 $ jux rev $ sound "{bd bd*4, sn:3 gabba*2}"

d5 $ every 2 (slow 2) $ jux rev $ smash 4 [1,2,3,4] $ sound "~ off"
  # gain "1.1"
  # speed "[2 4]/4"

d6 $ jux rev $ chop 16 $ every 2 (0.25 <~) $ every 4 (# n "2") $ slow 2 $ sound "onoff(3,8)"

d7 $ jux rev $ sound "funky*8" # n (irand 8)



d9 $ jux rev $ every 2 (# speed "0.5") $ slow 4 $ n (offadd 0.125 12 $ offadd 0.25 7 $ slow 2 $ "f8(3,8) a8(3,8) c8(3,8) e8(3,8)") # sound "xx"
  # room (scale 0.2 0.8 $ slow 8 sine1)
  # size (scale 0.2 0.8 $ slow 8 sine1)
  # gain "0.7"

d1 $ jux rev $ runWith 4 (# speed "2") $ loopAt 2 $ chop 16 $ sound "bsbass"
  # resonance "0.2"
  # gain "1.1"

d3 $ iter 4 $ jux rev $ chop 8 $ brak $ sound "{kurt kurt*2, ~ ~ alphabet}" # n (slow 4 $ run 26) # speed "[1,1.01,0.5]"


hush

cps 1.18


d1 $ every 4 (density 2) $ sometimes (# speed "2") $ every 4 (const (sound "bd*4")) $ slow 2 $ n "d6 ~ a6 d7 . d8 ~ a7 d7" # sound "[jx, xx]"
  # resonance "0.1"


d2 $ jux rev $ iter 4 $ sound "voodoo*8" # n (run 8)
  # gain "1.2"

