

d1 $ (slow 4 $ n (slowspread (offadd 0.125) [7,-5] $ "e3(3,8) f3(5,8,[2 4 6]/3)") # s "xx")
  # room (slow 4 sine1)
  # size "0.5"

d2 $ sound (unwrap $ e <$> (slow 8 $ run 8) <*> "8" <*> (pure "kurt"))
  # orbit "1"
  # vowel "e"
