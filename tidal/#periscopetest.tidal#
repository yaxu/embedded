d1 $ (jux rev $ off 0.125 (|*| speed "2") $ n (slowspread (offadd 0.25) [5,7,-12] "{d3 e2 g1], d3 [~ f3] a4}%1") # s "jx" |*| speed "0.5")
  # cutoff (slow 4 $ scale 250 2000 sine1)
  # resonance "0.3"
  # room "0.3"
  # size "0.3"

d2 $ runWith 4 (density 4) $ sound "bd8:4(3,8,[2 4])"
  # cutoff (slow 4 $ scale 150 2000 sine1)
  # resonance "0.3"
  # room "0.3"
  # size "0.3"
  # shape "0.9"
  