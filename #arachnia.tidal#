
d1 $ jux (rev . (# speed "2")) $ chop "<8 4 2>" $ n (every 4 (+2) $ struct "x(3,8,[0 2])" $ run 4) # sound "micron"
  # delay 0.8
  # delaytime (sine1 * 0.01)
  # delayfeedback "0.7"
  # cutoff (saw1 * 3000 + 1000)
  # resonance "0.2"

d2 $ n (off 0.25 (+7) $ "d7(3,8)") # sound "rash"
  


